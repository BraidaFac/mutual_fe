/**
 * Página de Trámites
 * Gestión de trámites y seguimiento
 */

import TramitesContent from "@/components/tramites/TramitesContent";

export const metadata = {
  title: "Trámites | CRM",
  description: "Gestión y seguimiento de trámites",
};

export default function TramitesPage() {
  return <TramitesContent />;
}
